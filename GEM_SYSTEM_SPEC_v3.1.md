# GEM - AI 全能動畫工作室：系統架構與 API 整合規格書

版本：v3.1
狀態：最終定稿版（含既有調整與實作校正）

本版本以你提供的 v3.0 為基礎，保留既有架構與提示詞精神，並正式納入目前已確認的產品行為與防呆修正，避免規格與實作脫節。

## 一、系統定位與核心技術棧

專案代號：GEM（Generative Engine for Motion）

系統定位：
全自動化、一站式 AI 動靜態影音工作流 Web App。支援「原創故事動畫（GEM）」與「商業產品廣告（PROMO）」雙軌生成模式，供內部短影音企劃、設計、剪輯與後製團隊使用。

核心 AI 模型整合清單：

- 文本與邏輯：Gemini 1.5 Pro 或同等級 Gemini 文本模型，負責劇本拆解、分鏡規劃、系統參數 JSON 精準轉譯
- 靜態影像：Midjourney、Nano Banana 2、Nano Banana Pro
- 動態與轉場：影片生成 API（如 Veo / Runway Gen-3 / Luma）
- 語音與配樂：ElevenLabs API（人聲）、Suno API（配樂）
- 對嘴同步：Hedra API / SyncLabs API

設計原則：

- 優先追求工作流穩定性與可編修性，不做過度自動化
- 所有高成本生成步驟需保留人工確認點
- 系統需允許企劃在每一個關鍵節點進行 Human-in-the-Loop 微調

## 二、全站 UI/UX 視覺與互動規範

視覺風格：
Cyberpunk Tech-Vibe。元件 Hover 帶有全息投影閃爍效果，載入進度以 Data Matrix 式數據流呈現。

版面配置：

- 大廳（Page 1）：畫面中央提供雙軌選擇按鈕，Route A 為 GEM、Route B 為 PROMO
- 工作區（Page 2 開始）：左側約 20% 為進度儀表板，右側約 80% 為核心操作艙
- 進度儀表板狀態規則：當前節點為橘色呼吸燈、已完成為恆亮、未進行為暗灰

補充 UI 規範：

- `/api-key` 為獨立設定頁，僅用於輸入、替換、清除 API Key
- 使用者不需要先輸入 API Key 才能進入網站
- `/admin`、`/archive`、首頁等公共頁面不可被 API Key 守門機制阻擋

## 三、雙軌頁面架構與系統流程詳細拆解

### Route A：GEM 故事動畫流

#### [節點 A1] 故事發想與防呆

串接 Gemini 產出故事大綱、劇本與分鏡。

強制規則：

- 每一句對話段落間必須強制注入 `\n\n`
- 此機制為後續語音系統精準切割音軌的必要條件，不可關閉

#### [節點 A2] 提示詞轉譯與 HITL

Gemini 將角色設計轉為 Midjourney 提示詞，並以 JSON 格式輸出。

前端要求：

- 必須提供可編輯文字方塊
- 企劃可在送出前手動微調提示詞與結構化欄位

#### [節點 A3] 角色擴充與場景定調（依序執行）

執行順序必須鎖定如下：

1. 角色初生：呼叫 Midjourney Text-to-Image 生成單一基準角色圖，提供確認與重新生成按鈕
2. 視角擴展：將確認後的角色圖送入 Nano Banana 2 做 Image-to-Image 擴展，產出 4 宮格概念圖與多角度三視圖
3. 純淨底圖：呼叫 Midjourney 生成純空景，後端需自動加入或過濾 `--no character, people` 等限制，確保無人物

此順序不可顛倒。

#### [節點 A4] 視覺合成

呼叫 Nano Banana 2 Composition 進行人物與場景合成。

前端要求：

- 提供遮罩塗抹工具
- 提供融合度 `control_strength` 參數拉桿
- 合成結果需提供確認與重生成入口

#### [節點 A5] 動態生成

將最終靜態圖送入影片 API 生成 5 到 10 秒片段。確認後跳轉至匯流點 Page 9。

---

### Route B：PROMO 產品廣告流

#### [節點 B1] 產品解構與腳本

左側輸入產品資訊，例如名稱、成分、功效、核心概念、風格與總時長。Gemini 產出包含首幀、尾幀、動態提示詞與轉場提示詞的 JSON。

強制規則：

- 對話與旁白間同樣必須空一行
- 輸出需保留可供前端編輯的結構化欄位

#### [節點 B2] 視覺注入與首尾生成

頂部上傳實拍產品墊圖。串接 Nano Banana Pro，於分割畫面生成各分鏡的 Start Frame 與 End Frame，具備圖片鎖定機制。

本節點的互動規則已校正如下：

- 進入頁面時不得自動開始生成圖片
- Start Frame 必須由使用者手動按下生成
- End Frame 必須在 Start Frame 生成並確認後，才允許手動生成
- Start Frame 與 End Frame 皆需具備確認、重新生成、下載功能
- 圖片鎖定後，除非使用者手動解除或重新生成，否則不得被系統背景流程覆蓋

#### [節點 B3] 動態插幀

結合首尾幀圖片 URL 與動態提示詞，呼叫影片 API 生成精準產品動態變化。

容錯規範：

- 若影片模型本身不支援 `countTokens`，不得因此中斷影片生成
- 系統需允許以降級估算方式記錄成本與用量，並優先保證影片生成流程完成

#### [節點 B4] 轉場縫合

時間軸節點套用轉場提示詞，擷取前後片段生成過渡特效並與主片段無縫縫合。確認後跳轉至匯流點 Page 9。

---

### 雙軌匯流點：聲音與輸出

#### [節點 M1] 音畫整合

呼叫 ElevenLabs 生成語音，Suno 生成配樂。

#### [節點 M2] 對嘴與最終導出（Page 9）

時序依賴防護：

- 對嘴 API 必須實作 `Promise.all()` 或等價檢查
- 確保影片 MP4 與音軌檔案皆已產出後，才可進入對嘴與最終合成

時長對齊防護：

- 若影音時長不符，預設套用凍結幀延長或音訊淡出
- 最終輸出的 MP4 時間軸必須完成對齊後才開放下載

## 四、後端架構與容錯機制設計指引

### 1. 彈性配置變數（Dynamic Variables）

API Keys 與 Endpoints：

- 絕對不可寫死於核心程式碼
- 允許透過 `.env` 或內部簡易管理員面板管理
- 目前產品規範允許另設獨立 API Key 管理頁，由使用者手動輸入與替換
- API Key 設定頁不得阻擋整站進入，只作為設定入口

System Prompts 樣板：

- 所有 Gemini 角色提示詞與腳本規範需以 Template 形式保存
- 應存於資料庫或等價可維護配置區，允許團隊微調

用量監控儀表板：

- 後台需建立 API 呼叫消耗量監控
- 需具備單日 Request 軟上限（Rate Limit）設定
- 需防禦無限重試導致的超額計費
- 儀表板數據不可因頁面切換、回到首頁、切換專案或重置專案而歸零
- 儀表板需支援自訂日期區間查詢

### 2. 強制鎖死邏輯（Hardcoded Constraints）

JSON 輸出清洗（防當機）：

- 接收 Gemini JSON 時，後端必須用 Regex 或等價清洗邏輯剔除 Markdown 標籤，例如 ```json
- 若解析異常需自動重試
- 若連續失敗，必須降級為前端手動編輯模式
- 嚴禁直接將系統打成白屏或死機錯誤

斷行語音切割（精準度）：

- 將文本送至 ElevenLabs 時，必須寫死規則：遇到 `\n\n` 就強制切斷為獨立音軌

非同步輪詢機制（防逾時）：

- 所有耗時影像生成 API，例如 Midjourney、Nano Banana、Video APIs，禁止使用固定短時間 HTTP Timeout 作為唯一完成判定
- 必須採用 Webhook、Task Queue、長任務輪詢或等價機制
- 前端需搭配 Data Matrix 動畫提供輪詢中的狀態回饋

內容安全阻斷攔截（NSFW Filter）：

- 必須攔截外部 API 的敏感詞阻擋錯誤
- 轉換為友善前端提示，例如「偵測到敏感詞彙，畫面生成遭 API 阻擋，請修改指令」

文字與 Logo 疊加防護：

- 系統生成影片只負責無文字空景或裸包裝動態
- 前端需明確提示：字體與品牌 Logo 應於輸出後或透過獨立靜態圖層疊加，避免 AI 直接生成導致文字扭曲

公共頁面可用性：

- `/admin`、`/archive`、首頁與 API Key 設定頁皆屬公共頁面
- 公共頁面不得因流程側欄、背景層級、API Key 狀態而無法顯示內容

## 五、附錄：商業廣告（PROMO）腳本提示詞規範樣板

註：此段落應作為 Gemini System Prompt 或 Prompt Template 存入資料庫。

你是一位專業的短影音企劃與導演，精通產品行銷與 AI 影片生成工具的視覺指令撰寫。請依據以下提供的產品資訊與設定，規劃出一套引人入勝的影片腳本與詳細分鏡表。

### 腳本撰寫嚴格規則

指令精細化：

- 視覺指令（Visual Prompt，首幀）與收尾提示詞（Closing Prompt，尾幀）必須使用英文撰寫，並附上中文翻譯

4 大強制元素：

- 開頭與收尾提示詞必須極度詳細
- 強制包含場景地點（Setting / Location）、光線（Lighting）、風格（Style）、鏡頭（Camera / Lens）

畫面延續性（Visual Continuity）：

- 收尾提示詞必須是首幀視覺指令的直接延續
- 只能改變物體最終狀態或攝影機最終位置

轉場邏輯：

- 每個分鏡結束前，必須提供銜接下一卡的轉場邏輯與具體轉場提示詞

排版防呆：

- 字幕或旁白中，每一句對話或旁白之間都必須空一行

節奏約束：

- 各分鏡秒數總和必須精準符合總時長
- 敘事與畫面必須確保前 3 秒抓住注意力

語氣要求：

- 開場企劃說明需帶有 Gemini 自然對話感
- 語氣自然、友善、專業，像資深企劃同事在與團隊溝通
- 遇到複雜需求時，可適度展示思考脈絡，但不可冗長失焦

### 輸入參數

產品：[請輸入產品名稱]

核心概念：[請輸入想傳達的核心價值或賣點]

風格設定：[請輸入視覺與節奏風格]

總時長：[請輸入預計總秒數]

### 強制輸出格式

💡 企劃思路與開場
[請以自然、友善且專業的語氣，用 2 到 3 句話向團隊解釋為什麼選擇這個創意切入點，以及它如何精準打中產品的核心概念。]

🎬 故事大綱
[請用 3 到 5 句話精煉描述影片的故事線與起承轉合，讓團隊能快速掌握畫面與情緒節奏。]

🎞️ 分鏡列表

分鏡 1

分鏡大綱：
[用 1 到 2 句話簡述此分鏡的畫面重點與動作，讓團隊能秒懂畫面]

畫面秒數：
[預計時長，例如 3 秒]

視覺指令 (EN)：
[首幀畫面，需包含場景、光線、風格、鏡頭]

中文翻譯：
[請輸入中文翻譯]

鏡頭設定：
[例如特寫（CU）、推軌鏡頭（Tracking in）、第一人稱視角（POV）]

聲音設定：
[背景音樂節奏、具體音效（SFX）]

字幕/旁白：
[第一句對話或旁白]

[第二句對話或旁白]

收尾提示詞 (EN)：
[尾幀畫面，必須延續首幀的場景、光線、風格與鏡頭，只改變最終動作或焦段狀態]

中文翻譯：
[請輸入中文翻譯]

首尾幀畫面連貫大綱：
[說明首幀到尾幀之間，畫面主體或運鏡發生了什麼連貫性的動態變化]

首尾幀畫面連貫提示詞：

提示詞 (EN)：
[給 AI 的連貫動作指示，例如 Smooth transition from tracking in to a complete stop...]

中文翻譯：
[請輸入中文翻譯]

轉場邏輯與提示詞：

邏輯說明：
[說明此分鏡與下一卡的銜接邏輯]

轉場提示詞 (EN)：
[具體的轉場視覺指令，例如 Fade in from white, seamless match cut...]

中文翻譯：
[請輸入中文翻譯]

分鏡 2

[依此類推]
